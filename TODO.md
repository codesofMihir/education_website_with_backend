# Faculty Dashboard Redesign - Task List

## Steps
- [x] 1. Update `myapp/views.py` — enhance `fcdash` view with per-course video/note/quiz counts and a `quizzes_count` stat
- [x] 2. Rewrite `myapp/static/style/faculty.css` — modern design system (CSS variables, sidebar, gradient stat cards, badges, responsive layout)
- [x] 3. Rewrite `myapp/templates/pages/facultydashboard.html` — sidebar + main content, stat cards, recent uploads table with badges, course management cards, mobile menu JS
- [ ] 4. Test: run `python manage.py runserver`, verify `/facultydashboard/` renders correctly

